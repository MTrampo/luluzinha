export const PUBLIC_API_ROUTES = ["/api/webhooks"] as const;

export const PUBLIC_PATHS = [
  "/",
  "/entrar",
  "/cadastrar",
  "/assinatura",
  "/documento/termo",
  "/documento/politica",
  "/manifest.webmanifest",
  "/manifest.json",
  "/robots.txt",
  "/sitemap.xml",
] as const;

export const AUTH_PATHS = ["/entrar", "/cadastrar"] as const;

export const ROUTE_PAINEL = "/painel" as const;
export const ROUTE_ENTRAR = "/entrar" as const;
export const ROUTE_CADASTRAR = "/cadastrar" as const;
export const ROUTE_ASSINATURA = "/assinatura" as const;
export const ROUTE_CONVITE = "/convite" as const;
